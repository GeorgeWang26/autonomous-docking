1. add abort in phase 2, if euclidean distance greater than calculated ty by 1m
2. name random values to constants
3. use a action to active docking
4. eddit message to have a list in case there are multiple tags in one frame, make a new tag message every loop in tags
5. update is_charging with canbus